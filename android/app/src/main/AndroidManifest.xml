<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".SophiaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sophia"
        android:usesCleartextTraffic="false">

        <!--
          singleTask: a course link or a notification used to start a second copy of
          MainActivity on top of the running one, so back left the user in a stale
          duplicate of the app. With one task, `onNewIntent` delivers the link to the
          instance that is already open.

          configChanges: Compose reads the new configuration itself, and letting the
          activity survive a rotation, a theme switch or a font-size change is what keeps
          the reader on its page and the onboarding on its step.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|fontScale|density|locale|layoutDirection"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Sophia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sophia" android:host="course" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".data.TrialReminderReceiver"
            android:exported="false" />
    </application>
</manifest>
